<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="index.css">
</head>
<body>
    <div class="content">
        <form action="/" method="POST">
            <label for="ma_sp">Mã SP:</label>
            <input type="text" id="ma_sp" name="ma_sp" required>
            <br>
            <label for="ten_sp">Tên SP:</label>
            <input type="text" id="ten_sp" name="ten_sp" required>
            <br>
            <label for="so_luong">Số Lượng:</label>
            <input type="number" id="so_luong" name="so_luong" required>
            <br>
            <button type="submit">Thêm Sản Phẩm</button>
        </form>
        <form action="/delete" method="POST">
            <table border="1">
                <tr>
                    <th>Mã SP</th>
                    <th>Tên SP</th>
                    <th>Số Lượng</th>
                    <th>Chọn</th>
                </tr>
                <% for (let i = 0; i < SanPham.length; i++) { %>
                <tr>
                    <td><%= SanPham[i].ma_sp.S %></td>
                    <td><%= SanPham[i].ten_sp.S %></td>
                    <td><%= SanPham[i].so_luong.N %></td>
                    <td><input type="checkbox" name="ma_sp" value="<%= SanPham[i].ma_sp.S %>"></td>
                </tr>
                <% } %>
            </table>
            <button type="submit">Xóa</button>
        </form>
    </div>
</body>
</html>